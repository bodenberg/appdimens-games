/**
 * @author Bodenberg
 * GIT: https://github.com/bodenberg/appdimens.git
 */
package com.example.app.views.java.pt;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;
import androidx.databinding.DataBindingUtil;
import com.appdimens.dynamic.code.AppDimens;
import com.example.app.R;
import com.example.app.databinding.ActivityInterpolatedExampleBinding;

public class InterpolatedExampleActivity extends AppCompatActivity {
    @Override protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        ActivityInterpolatedExampleBinding b = DataBindingUtil.setContentView(this, R.layout.activity_interpolated_example);
        b.setBaseValue(48f);
        b.getTvResult().setText("INTERPOLATED: 48dp → " + (int)AppDimens.from(48f).interpolated().toDp(getResources()) + "dp");
    }
}

