/**
 * @author Bodenberg
 * GIT: https://github.com/bodenberg/appdimens.git
 */
package com.example.app.views.java.en;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;
import androidx.databinding.DataBindingUtil;
import com.appdimens.dynamic.code.AppDimens;
import com.example.app.R;
import com.example.app.databinding.ActivityFitExampleBinding;

public class FitExampleActivity extends AppCompatActivity {
    @Override protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        ActivityFitExampleBinding b = DataBindingUtil.setContentView(this, R.layout.activity_fit_example);
        b.setBaseValue(48f);
        b.getTvResult().setText("FIT: 48dp → " + (int)AppDimens.from(48f).fit().toDp(getResources()) + "dp");
    }
}

