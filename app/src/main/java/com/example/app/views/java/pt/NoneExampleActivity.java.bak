/**
 * @author Bodenberg
 * GIT: https://github.com/bodenberg/appdimens.git
 */
package com.example.app.views.java.pt;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;
import androidx.databinding.DataBindingUtil;
import com.appdimens.dynamic.code.AppDimens;
import com.example.app.R;
import com.example.app.databinding.ActivityNoneExampleBinding;

public class NoneExampleActivity extends AppCompatActivity {
    @Override protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        ActivityNoneExampleBinding b = DataBindingUtil.setContentView(this, R.layout.activity_none_example);
        b.setBaseValue(48f);
        b.getTvResult().setText("NONE: 48dp → " + (int)AppDimens.from(48f).none().toDp(getResources()) + "dp (constante)");
    }
}

